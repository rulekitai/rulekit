// Every claim must trace to the corpus. A model that can read the open web will ground
// an answer in a page nobody chose and cite it as though it were the rules.
import { disableTool } from "eve/tools"

export default disableTool()
