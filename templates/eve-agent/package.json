{
  "name": "rulekit-eve-agent",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "The same rules agent, running on Vercel Eve instead of the AI SDK.",
  "scripts": {
    "dev": "eve dev",
    "build": "eve build",
    "start": "eve start",
    "deploy": "eve deploy"
  },
  "dependencies": {
    "@rulekitai/rulekit": "workspace:*",
    "ai": "^7.0.58",
    "eve": "^0.44.4",
    "zod": "^4.4.0"
  },
  "engines": {
    "node": ">=24"
  }
}
